#!/bin/bash
# Basic script template that can be copied to make new scripts that already start with the shell declaration, end with exit, and have the executable permission bit set.
echo "This is a script."
exit
